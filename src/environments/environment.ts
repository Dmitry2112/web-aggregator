import { IEnvironment } from './environment.interface';

export const environment: IEnvironment = {
    production: true,
    apiUrl: 'http://localhost:5000' //TODO: Здесь нужно будет добавить адрес удаленного сервера
};
